<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.HomeAutomation" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryVariant">@android:color/darker_gray</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l"> #0172A9</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.App" parent="Theme.HomeAutomation">
        ...
        <item name="sliderStyle">@style/Widget.AppCompat.SeekBar</item>
    </style>

    <style name="SwitchCompatTheme" >
        <item name="colorControlActivated">@color/radio</item>
    </style>

</resources>